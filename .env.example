# Porta do servidor dashboard
PORT=3000

# Diretório base para autenticação do WhatsApp
# Se não especificado, será usado .whatsapp-auth na raiz do projeto
AUTH_BASE_DIR=.whatsapp-auth

# Host do dashboard (padrão: localhost)
# Use 0.0.0.0 para permitir acesso externo
DASHBOARD_HOST=localhost

# Nível de log (silent, error, warn, info, debug)
LOG_LEVEL=silent

# Timeout para reconexão automática (em milissegundos)
RECONNECT_TIMEOUT=5000

# Intervalo de atualização do dashboard (em milissegundos)
DASHBOARD_UPDATE_INTERVAL=1000

# Habilitar CORS no dashboard (true/false)
ENABLE_CORS=true

# Domínios permitidos para CORS (separados por vírgula)
# Exemplo: http://localhost:3000,https://meusite.com
CORS_ORIGINS=*

# Configurações opcionais para múltiplas instâncias
# Prefixo para IDs de instâncias (opcional)
INSTANCE_PREFIX=

# Número máximo de instâncias simultâneas (0 = ilimitado)
MAX_INSTANCES=0
